<% title 'Resource Library' %>
<%= render partial: 'shared/resource/primary', locals: { resource: Resource.published.order(:created_at).first.decorate } if Resource.published.first %>
<%= render partial: 'shared/resources', locals: { resources: Resource.published.order(:created_at).page(1).offset(1).decorate } %>
