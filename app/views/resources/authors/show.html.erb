<% title params[:resource_type] ? "#{params[:resource_type].pluralize.titleize} by #{author.name}" : "Resources by #{author.name}" %>
<%= render 'shared/listing_header' %>
<%= render partial: 'shared/resources', locals: { resources: resources } %>
