<% title params[:resource_type] ? "#{params[:resource_type].pluralize.titleize} on #{scripture.name}" : "Resources on #{scripture.name}" %>
<%= render 'shared/listing_header' %>
<%= render 'shared/resources', locals: { resources: resources } %>
