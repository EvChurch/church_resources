<% title params[:resource_type] ? "#{params[:resource_type].pluralize.titleize}" : 'Recent Resources' %>
<%= render 'shared/listing_header' %>
<%= render 'shared/resources', locals: { resources: resources } %>
